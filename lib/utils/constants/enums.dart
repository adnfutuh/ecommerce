enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymenyMethods {
  paypal,
  googlePlay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm,
}
